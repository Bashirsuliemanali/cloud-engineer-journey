Day 80

Python session 1 today. First proper structured Python since 
starting the journey.

Covered variables, just boxes with labels. Strings need quotes, 
integers don't, booleans are True or False. Used f-strings to 
mix variables into text cleanly, way better than joining strings 
manually.

Lists store values in order, access them by index starting from 
0. Dictionaries store key value pairs like a phone book, you 
look up by name not by position.

Loops wrote a for loop that went through every region in a 
list and printed a deployment message for each one. One line of 
code, three outputs.

Functions with def — write the code once, call it whenever you 
need it. Built a check_server function that takes a name and 
status and prints an alert if the server is unhealthy.

